{
  "at": "2026-07-14T12:44:47.128Z",
  "by": "probes/probe-watches.mjs",
  "note": "THE READING for the watches. merge.js reads this. If it is stale or absent, the watch is UNPROVEN — amber — no matter that the probe file exists. A probe that has never run is a prayer.",
  "watches": {
    "COST WATCH": {
      "id": "COST WATCH",
      "status": "OK",
      "ok": 2,
      "warn": [],
      "fail": [],
      "note": "a normal chunk still speaks (200) · 10,000 chars REFUSED by the wall (text_too_long)"
    },
    "DATA WATCH": {
      "id": "DATA WATCH",
      "status": "FAIL",
      "ok": 3,
      "warn": [],
      "fail": [
        "THE ANON KEY MAY WRITE TO `subscribers` (204). The \"unsub by token\" policy uses `using (true)` and never checks the token. Nothing was changed by this probe — the nil UUID matches no row — BUT A REAL FILTER WOULD HAVE GONE THROUGH. With the public key alone, anyone can unsubscribe your entire list, or REWRITE EVERY SUBSCRIBER EMAIL and redirect the newsletter. DROP THAT POLICY. Do the unsubscribe through a SECURITY DEFINER function that takes the token and can do nothing else."
      ],
      "note": "AN OPEN DOOR. THE ANON KEY MAY WRITE TO `subscribers` (204). The \"unsub by token\" policy uses `using (true)` and never checks the token. Nothing was changed by this probe — the nil UUID matches no row — BUT A REAL FILTER WOULD HAVE GONE THROUGH. With the public key alone, anyone can unsubscribe your entire list, or REWRITE EVERY SUBSCRIBER EMAIL and redirect the newsletter. DROP THAT POLICY. Do the unsubscribe through a SECURITY DEFINER function that takes the token and can do nothing else."
    },
    "TREASURY WATCH": {
      "id": "TREASURY WATCH",
      "status": "OK",
      "ok": 2,
      "warn": [],
      "fail": [],
      "note": "/quiz/start returns no answer key · /quiz/submit refused a forged session (401)"
    },
    "HULL WATCH": {
      "id": "HULL WATCH",
      "status": "WARN",
      "ok": 0,
      "warn": [
        "instrument is a stub"
      ],
      "fail": [],
      "note": "Hull integrity is checked by comparing fleet-structure.json file hashes against a committed baseline. That comparison is not yet wired. Reporting WARN, not OK — a stub that returned green would be the very lie this whole system exists to kill."
    },
    "ARCHIVE WATCH": {
      "id": "ARCHIVE WATCH",
      "status": "OK",
      "ok": 6,
      "warn": [],
      "fail": [],
      "note": "6/6 HIT · 0 renders · $0.00 · passage 27e9c5afcdc4 · the model string, the voice, the style string and BOTH chunkers are unchanged"
    }
  }
}
